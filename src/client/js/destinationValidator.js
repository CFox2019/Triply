/**
 * Used to validate the destination input.
 *
 * @param {string} destination The destination string to validate
 */
export function checkIfValidDestination(destination) {
    console.log("::: Running checkIfValidDestination :::", destination)

    return destination.length > 0
}
